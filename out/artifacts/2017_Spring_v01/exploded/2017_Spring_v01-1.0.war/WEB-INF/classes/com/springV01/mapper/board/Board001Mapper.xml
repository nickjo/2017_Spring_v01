<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springV01.mapper.Board001Mapper">

    <!-- 게시판 조회-->
    <select id="board001R1" parameterType="map" resultType="map">
        select bno,
               title,
               content,
               writer,
               regdate,
               viewcnt
          from tbl_board
    </select>

</mapper>